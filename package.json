{
  "name": "keycloak-client",
  "version": "6.0.1",
  "description": "A js keycloak client based of the official one, but adds a 'capacitor' adapter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "Apache 2.0",
  "dependencies": {
    "@capacitor/core": "^1.1.0",
    "base64-js": "^1.3.0",
    "js-sha256": "^0.9.0"
  }
}
